```{=html}
<style type="text/css">
.grid-gallery {
  columns: 4 250px;
  column-gap: 0.5rem;
  width: 90%;
  margin: 0 auto;
}
.grid-item {
  width: 200px;
  margin: 0 0.5rem 0.5rem 0;
  display: inline-block;
  width: 100%;
  border-radius: 5px;
}
img.grid-item {
  width: 100%;
  transition: all .25s ease-in-out;
}
img.grid-item:hover {
  transform: scale(2.0);
}
</style>
```

```{ojs}
exif = require("exifr")
getDesc = (path) => exif.parse(path).then((data) => data["ImageDescription"])
```

::: {.grid-gallery}
<% for (const item of items) { %>
::: {}
![](<%= item.path %>){.grid-item .lightbox loading="lazy" group="quarto-grid-gallery"}
:::

:::
<% } %>
:::

